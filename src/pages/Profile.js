import React from 'react'
import Header from '../components/Header'
import Navigation from '../components/Navigtion'


export default function Profile() {

    //will need to use Owener Id for each owner

    return (
        <>
        <Header />
        <Navigation />
        <p>All owners will need uniquie id </p>
        <h2>A Username</h2>
        <h3>Scrolling Bar like Netflix or Amazon with pic of pet is provided and the names of users available with an Event listener if clicked goes to that pet </h3>
        </>
    )
}